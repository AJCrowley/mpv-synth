# Enable or disable the script
enabled=yes

# All values in the list must match for the subtitle track to be
# selected (not case sensitive). The order of the terms does not matter.
# The search is performed on the track's readable title, not its code.
# Examples:
# english,forced - matches tracks with both "English" and "forced" in the title
match=en,forced